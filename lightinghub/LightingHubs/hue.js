export default function hueController(modifier, data) {
    if (modifier, data){
        switch (modifier){

            
            default: return 
        }
    }
    return console.log('YAYAYAYYAYA', data);
}